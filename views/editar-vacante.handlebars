<main class="contenedor">
    <form action="/vacantes/editar/{{vacante.url}}" method="POST" class="default-form">

        <h3>Información General</h3>

        <div class="campo">
            <label>Titulo</label>
            <input type="text" value="{{vacante.titulo}}" name="titulo" placeholder="Ej: React Developer" required>
        </div>

        <div class="campo">
            <label>Empresa</label>
            <input type="text" value="{{vacante.empresa}}" name="empresa" placeholder="Empresa Contrata" required>
        </div>

        <div class="campo">
            <label>Ubicación</label>
            <input type="text" value="{{vacante.ubicacion}}" name="ubicacion" placeholder="Ej: Ciudad de México"
                required>
        </div>

        <div class="campo">
            <label>Salario</label>
            <input type="text" value="{{vacante.salario}}" name="salario" placeholder="Ej: $10,000 USD">
        </div>

        <div class="campo">
            <label>Contrato</label>
            <select name="contrato" required>
                {{#tipoContrato vacante.contrato}}
                <option value="" disabled selected>-- Seleccione --</option>
                <option value="Freelance">Freelance</option>
                <option value="Tiempo Completo">Tiempo Completo</option>
                <option value="Medio Tiempo">Medio Tiempo</option>
                <option value="Por Proyecto">Por Proyecto</option>
                {{/tipoContrato}}
            </select>
        </div>
        <h3>Descripción del Puesto</h3>
        <div class="campo descripcion">
            <label>Descripción</label>
            <input type="hidden" id="x" value="{{{vacante.descripcion}}}" name="descripcion">
            <trix-editor input="x"></trix-editor>
        </div>

        <h3>Conocimientos</h3>

        <ul class="lista-conocimientos">
            {{#seleccionarSkills vacante.skills}}
            {{/seleccionarSkills}}
        </ul>

        <div class="campo centrar-horizontal">
            <input type="hidden" name="skills" id="skills">
            <input type="submit" class="btn btn-azul" value="Guardar Cambios">
        </div>

    </form>
</main>